//import actions
import {} from '../store/actions'


//initialState
const initialState = {
}

export const reducer = (state = initialState, action) => {
     switch(action.type) {
          default:
               return state
     }
}